services:
  hr-bot:
    build: .
    container_name: hr_filter_bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./logs:/app/logs
      - ./fallback_log.jsonl:/app/fallback_log.jsonl
